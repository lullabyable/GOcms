name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger deploy
        run: curl -s --max-time 15 "http://${{ secrets.DEPLOY_HOST }}:8080/deploy?token=gocms-deploy-2026"
